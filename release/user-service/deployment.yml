apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      restartPolicy: Always
      containers:
        - name: user-service
          image:  982534381318.dkr.ecr.ap-southeast-2.amazonaws.com"/group18/devops_project:user_service
          resources:
            requests:
              cpu: 10m
              memory: 100Mi
            limits:
              cpu: 200m
              memory: 500Mi
          command: ["/bin/sh", "-c"]
          args: ["npm run start"]
          envFrom:
            - secretRef:
                name: user-service-secret
